// Simple test file to verify our import fixes
import React from "react";
import MessageArea from "./messageArea";

const MessageAreaTest = () => {
  return (
    <div>
      <h2>MessageArea Test</h2>
      <p>This is a simple test component to verify our import fixes.</p>
    </div>
  );
};

export default MessageAreaTest;
